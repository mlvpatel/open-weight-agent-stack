{
  "name": "open-weight-agent-stack",
  "version": "1.1.0",
  "private": true,
  "description": "Build tooling for the Open-Weight Agent Stack manual: diagram rendering and site assets.",
  "license": "CC-BY-4.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "render": "bash scripts/render_diagrams.sh",
    "sbom": "npm sbom --sbom-format cyclonedx > sbom.cdx.json"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "11.16.0",
    "puppeteer": "25.5.0"
  }
}
